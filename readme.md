# big brother

Lightweight server monitoring with a real-time web dashboard.

Two components:

- **hub** — the web application and API
- **agent** — collects system metrics and streams them to the hub

Work in progress. See the `main` branch for the current state.
