1. Get Big Brother Agent Status
   kubectl get daemonset -n {{ .Release.Namespace }} {{ include "bigbrother-agent.fullname" . }}
   kubectl get pods -n {{ .Release.Namespace }} -l app.kubernetes.io/name={{ include "bigbrother-agent.name" . }}
2. View Agent Logs
   kubectl logs -n {{ .Release.Namespace }} -l app.kubernetes.io/name={{ include "bigbrother-agent.name" . }} -f
3. Verify SSH Key Configuration
   kubectl get secret -n {{ .Release.Namespace }} {{ include "bigbrother-agent.fullname" . }} -o jsonpath='{.data.ssh-key}' | base64 -d
4. Agent Configuration
   - Port: {{ .Values.env.PORT }}
   - Image: {{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}
   {{- if .Values.hostNetwork }}
   - Host Network: Enabled
   {{- end }}
   {{- if .Values.gpuRuntime }}
   - GPU Runtime: {{ .Values.gpuRuntime }}
   {{- end }}
5. Next Steps
   - Ensure the Big Brother Hub is accessible from the cluster
   - Check that the SSH key is registered with the Hub
   - Verify agent connectivity: kubectl logs -n {{ .Release.Namespace }} -l app.kubernetes.io/name={{ include "bigbrother-agent.name" . }}
For more information, visit: https://github.com/cyber-dev-skalovsi/big-brother/
