apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "BIGBROTHER-agent.fullname" . }}-test-connection"
  labels:
    {{- include "BIGBROTHER-agent.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "BIGBROTHER-agent.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
